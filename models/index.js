const { Product } = require("./product.model");

const { User } = require("./user.model");

const { New } = require("./new.model");

const { Order } = require("./order.model");

module.exports = { Order, Product, User, New };
